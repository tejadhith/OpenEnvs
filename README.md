# OpenEnvs

OpenEnvs is a workspace for building and organizing OpenEnv-compatible environments.

## Environments

- `envs/shader/` — OpenEnv-compatible environment for shader generation, repair, and multi-turn refinement. Includes a headless GLSL renderer (ModernGL + EGL) and subprocess-isolated rendering harness.
